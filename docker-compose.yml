services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: setwise
      POSTGRES_USER: setwise
      POSTGRES_PASSWORD: setwise
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
